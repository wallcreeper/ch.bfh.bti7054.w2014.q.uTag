/**
 * @ngdoc service
 * @name utag.utils:utils
 * @description
 * # utils
 * Service in the utag app.
 */
angular
.module('utag.utils')
.factory('utils', function utils($http, $log, API_PREFIX) {
	'use strict';

	return {

	};

});
